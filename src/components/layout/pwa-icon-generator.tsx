'use client';

import { useEffect } from 'react';

export default function PWAIconGenerator() {
  useEffect(() => {
    // This component can be used to generate PWA icons if needed
    // For now, we'll use the existing logo for all sizes
    console.log('PWA Icon Generator loaded');
  }, []);

  return null;
}
